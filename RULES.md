# قوانین مخزن VBA

1. **هیچ کاراکتر فارسی یا غیر ASCII داخل فایل‌های `.bas` مجاز نیست.** متن فارسی لازم باید با `ChrW` یا کد یونیکد ساخته شود.
2. همه ماژول‌ها با `Option Explicit` شروع شوند.
3. ماکروهای عمومی با پیشوند `RVA_` نام‌گذاری شوند.
4. برای Headingها فقط از Styleهای استاندارد Word یعنی `wdStyleHeading1` تا `wdStyleHeading3` استفاده شود.
5. ماکروهای مخرب باید هشدار واضح بدهند و روی کپی سند اجرا شوند.
6. ترجیح با `Range` است؛ `Selection` فقط در صورت ضرورت.
7. `On Error Resume Next` باید کوتاه و محلی باشد و با `On Error GoTo 0` بسته شود.
8. `ScreenUpdating` و `StatusBar` در مسیر موفق و خطا بازیابی شوند.
9. اعداد شیوه‌نامه فقط از منبع مشخص وارد Preset شوند؛ الزام ساختگی ممنوع.
10. بعد از تأیید اجرای موفق توسط کاربر، در ابتدای کد این کامنت اضافه شود: `' Tested on YYYY-MM-DD`.
11. برای کد قدیمی با تاریخ تست نامعلوم، فقط «legacy user-tested; date unknown» ثبت شود.
12. هر تغییر باید از `python scripts/check_vba_repo.py` عبور کند؛ تست استاتیک جای تست واقعی Word را نمی‌گیرد.
